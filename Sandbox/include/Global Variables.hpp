#pragma once

#include <cstdint>

#include "SFML/System/Vector2.hpp"

inline sf::Vector2f WINDOW_SIZE(1600.0f, 900.0f); // its rounded to uint

inline uint32_t MAX_BALLS = 2028;

inline sf::Vector2f BOUNDS_POSITION;
inline sf::Vector2f BOUNDS_SIZE(3200.0f, 1800.0f);
